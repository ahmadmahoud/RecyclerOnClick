<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.recycleronclick">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/Theme.RecyclerOnClick">
        <activity android:name=".single_select.SelectorActivity"></activity>
        <activity android:name=".rv_onclick_bundle_data.BndleShow" />
        <activity android:name=".rv_onclick_bundle_data.BandleActivity" />
        <activity android:name=".rv_normal_book.NormalActivity" />
        <activity android:name=".rv_with_binding.MainBinding" />
        <activity android:name=".rv_normal_book.ShowMathActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>